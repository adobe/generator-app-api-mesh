$schema: http://json-schema.org/draft-07/schema
$id: https://adobe.io/schemas/app-builder-templates/1

categories:
  - action
  - graphql-mesh

#Removing apis section - Discussed with litviach@adobe.com . As the api-mesh plugin is generating Adobe credentials so this can be removed for now
# apis:
#   - code: GraphQLServiceSDK

runtime: true
